[project]
name = "mccarroll-tw-clp"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = ["google-cloud-storage", "pyyaml"]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

